# Alice 项目依赖包
# 轻量化版本 - 优先保证响应速度和易用性

#  核心依赖 (必需)
jieba==0.42.1                  # 中文分词工具 (轻量、快速)

#  可选增强 (按需安装)
# LTP (完整版，用于深度句法分析和 NER 增强 - 首次加载较慢)
ltp==4.2.14

#  可选：transformers (用于更强大的 NER 模型)
# transformers==4.30.0
# torch==2.7.1

#  测试框架
pytest==7.4.0                  # 单元测试框架
pytest-cov==4.1.0              # 测试覆盖率

#  开发工具
black==23.7.0                  # 代码格式化
flake8==6.0.0                  # 代码质量检查

#  工具库
loguru==0.7.0                  # 现代化日志库
rich==13.4.2                   # 终端美化输出

#  以下为 Phase 2+ 可选依赖
# 语义相似度计算 (可选，增加响应智能度但增加体积)
sentence-transformers==2.2.2
torch==2.7.1
torchvision==0.22.1

# Web 服务 (可选)
flask==2.3.2
gunicorn==21.2.0

# 数据处理 (可选)
pandas==2.0.3
