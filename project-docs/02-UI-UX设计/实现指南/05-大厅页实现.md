# 大厅页实现

## 文档说明

本文档说明大厅页面的实现，包括规则说明、匹配界面、匹配动画等功能。

---

## 一、页面概述

### 功能说明

1. **规则说明** - 展示实验规则和积分机制
2. **开始匹配** - 点击后加入匹配队列
3. **匹配界面** - 显示匹配动画和等待信息
4. **取消匹配** - 可以取消匹配请求

---

## 二、组件结构

```
views/
└── Lobby.vue

components/
└── Lobby/
    ├── RulesSection.vue       # 规则说明
    └── MatchingSection.vue    # 匹配界面
```

---

## 三、核心代码

### Lobby.vue

```vue
<template>
  <div class="lobby-page">
    <!-- 规则说明 -->
    <RulesSection v-if="!isMatching" />
    
    <!-- 匹配界面 -->
    <MatchingSection
      v-else
      @cancel="handleCancel"
    />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useUserStore } from '@/stores/user'
import RulesSection from '@/components/Lobby/RulesSection.vue'
import MatchingSection from '@/components/Lobby/MatchingSection.vue'

const userStore = useUserStore()
const isMatching = ref(false)

function handleStartMatch() {
  isMatching.value = true
}

function handleCancel() {
  isMatching.value = false
}
</script>
```

### MatchingSection.vue

```vue
<template>
  <div class="matching-section">
    <div class="spinner"></div>
    <h2>正在匹配对手...</h2>
    <p>{{ statusMessage }}</p>
    
    <div class="progress-container">
      <div class="progress-bar">
        <div class="progress-fill" :style="{ width: progress + '%' }"></div>
      </div>
    </div>
    
    <button @click="emit('cancel')" class="btn btn-secondary">
      取消匹配
    </button>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted } from 'vue'

const emit = defineEmits<{
  (e: 'cancel'): void
}>()

const waitTime = ref(0)
const progress = ref(0)

const statusMessage = computed(() => {
  if (waitTime.value < 5) return '正在寻找对手...'
  if (waitTime.value < 10) return '稍等片刻，马上就好...'
  if (waitTime.value < 20) return '正在为您匹配最佳对手...'
  return '正在为您匹配对手...'
})

let timer: number | null = null

onMounted(() => {
  timer = window.setInterval(() => {
    waitTime.value++
    progress.value = Math.min(100, (waitTime.value / 30) * 100)
  }, 1000)
})

onUnmounted(() => {
  if (timer) clearInterval(timer)
})
</script>

<style scoped>
.matching-section {
  text-align: center;
  padding: 40px 20px;
}

.spinner {
  width: 60px;
  height: 60px;
  border: 4px solid #f3f3f3;
  border-top-color: #667eea;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin: 0 auto 30px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.progress-container {
  margin: 30px auto;
  width: 100%;
  max-width: 400px;
}

.progress-bar {
  height: 8px;
  background: #f3f3f3;
  border-radius: 4px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea, #764ba2);
  transition: width 0.3s;
}
</style>
```

---

## 四、关键功能

### 4.1 规则说明展示

使用折叠面板展示详细规则，保持页面简洁。

### 4.2 匹配状态管理

```typescript
// stores/session.ts
const status = ref<'matching' | 'chatting' | 'survey' | 'ended'>('matching')

function startMatching() {
  status.value = 'matching'
}
```

### 4.3 WebSocket连接

```typescript
// components/Lobby/MatchingSection.vue
const { connect, onMessage, disconnect } = useWebSocket()

async function handleStartMatch() {
  const response = await startMatch()
  connect(`${import.meta.env.VITE_WS_BASE_URL}/match/${response.request_id}`)
  onMessage('matched', handleMatched)
}
```

---

## 五、测试要点

- [ ] 规则说明完整显示
- [ ] 点击开始匹配进入匹配界面
- [ ] 匹配动画正常显示
- [ ] 进度条随时间更新
- [ ] 等待信息轮播显示
- [ ] 点击取消匹配返回大厅
- [ ] 匹配成功跳转聊天页

---

**文档版本**：1.0  
**创建日期**：2026-02-22  
**作者**：Cline