太棒了！将想法落地为具体的功能清单是开发的第一步。基于我们之前的讨论（特别是“真人vs真人”、“真人vs AI”的混排模式），我将这个网站的功能模块梳理如下。

你可以把这个清单直接发给 AI 编程助手（如 Cursor、Copilot）或者作为自己开发的 Checklist。

---

### 项目名称：图灵测试社交实验平台

#### 模块一：用户鉴权与准入
考虑到是学校内部测试，流程应尽量简化，但要保证数据可追溯。

1.  **邀请码登录页：**
    *   **输入框：** 6位左右的字母数字组合（你生成的100个密钥）。
    *   **校验逻辑：** 核对数据库中的密钥。若有效，标记为“已使用”并创建用户会话。
    *   **昵称设置：** 登录后要求用户设置一个匿名昵称（如“访客#892”或自定义），用于聊天显示。
    *   *目的：控制样本范围，防止无关人员进入，确保每个测试者都是有效数据。*

#### 模块二：大厅与匹配系统
这是实现“混入”策略的核心。

2.  **实验说明页：**
    *   简短告知：“这是一个研究性学习项目。你将随机匹配到一位对话者，可能是真人，也可能是AI。请通过聊天判断对方身份。”
    *   **“开始匹配”按钮：** 点击后进入队列。
3.  **匹配逻辑引擎：**
    *   **队列检测：** 检查是否有其他真人在等待。
    *   **路由策略：**
        *   情况A：有真人等待 -> 建立 **真人-真人** 连接。
        *   情况B：无真人等待（或为了控制比例强制分流） -> 建立 **真人-AI** 连接。
    *   **超时处理：** 如果等待超过30秒无真人，自动匹配AI（避免用户流失）。

#### 模块三：聊天交互界面
这是用户停留时间最长的页面，体验必须流畅。

4.  **对话窗口：**
    *   标准的聊天UI（气泡式对话）。
    *   显示对方的默认昵称（如“对话者A”）。
5.  **拟人化交互细节（关键！）：**
    *   **“正在输入...”提示：**
        *   真人：发送消息时，对方浏览器显示提示。
        *   AI：后端计算回复后，先推送“正在输入”状态，延迟1-2秒后再推送消息文本。
    *   **打字延迟：** AI不要秒回，根据回复字数模拟打字时间。
6.  **结束对话机制：**
    *   设置一个“结束对话”按钮。
    *   **防误判：** 设置最低对话轮数（如双方各发3句以上），否则提示“请多聊几句再结束哦”。

#### 模块四：问卷与真相揭晓
这是收集实验数据的核心环节。

7.  **即时问卷弹窗：**
    *   对话结束后立即弹出。
    *   **核心问题：** “你认为对方是？”（选项：真人 / AI / 不确定）。
    *   **辅助问题：** “对话流畅度评分”（1-5星）、“对方像真人的理由”（选填）。
8.  **真相揭晓页：**
    *   用户提交问卷后显示。
    *   **若对手是AI：** 显示“答对了！对方是改进版Eliza”或“可惜，对方其实是AI”。
    *   **若对手是真人：** 显示“对方确实是真人！”。
9.  **社交互动彩蛋：**
    *   在真人模式下，揭晓真相后，展示**对方的判断结果**（例如：“对方觉得你是AI，因为...”）。这能极大地增加趣味性和传播度。

#### 模块五：个人中心与统计
增加用户的参与感和留存率。

10. **战绩看板：**
    *   显示历史记录：参与了多少次对话。
    *   准确率：判断对了多少次（仅针对人机局统计）。
    *   经历一览：遇到过多少次真人，多少次AI。

#### 模块六：管理员后台
这是为你自己准备的工具，方便监控实验进度。

11. **数据看板：**
    *   当前在线人数。
    *   实时匹配情况（几对人正在聊天）。
12. **数据导出：**
    *   导出 CSV/Excel 表格，包含：
        *   每一场对话的完整日志。
        *   用户的判断结果。
        *   对话耗时。
13. **密钥管理：**
    *   生成新密钥、查看已使用密钥列表。

---

### 技术栈建议

鉴于你的服务器配置（4C8G）和高中生开发者的背景，推荐以下高性价比技术栈：

*   **后端：** **Python (FastAPI)** 或 **Node.js**。
    *   *理由：* Python 处理 NLP（你的 Eliza）非常方便；FastAPI 原生支持异步 WebSocket，性能极高，写法简单。
*   **前端：** **Vue.js 3** 或 **React**。
    *   *理由：* 组件化开发，生态好，容易写出漂亮的 UI。
*   **数据库：** **SQLite** 或 **PostgreSQL**。
    *   *理由：* 如果数据量不大（几百人），SQLite 最省事，一个文件搞定，备份方便。如果追求专业，上 PostgreSQL，你的服务器跑 PG 绑绑有余。
*   **通信协议：** **WebSocket**。
    *   *理由：* 实现真人聊天和“正在输入”提示的必需品。

### 开发优先级

1.  **MVP版本（最小可行性产品）：** 先跑通“用户登录 -> 匹配AI -> 聊天 -> 提交问卷”这一条线。
2.  **进阶版本：** 加入“真人匹配”逻辑和 WebSocket 支持。
3.  **完善版本：** 加入“正在输入”拟人化延迟、数据看板、真相揭晓彩蛋。

这个功能列表既满足了你的“图灵测试”科研需求，又具备了“社交产品”的潜质，非常适合寒假研究性学习项目！