# 前端功能修复完成报告

## 修复摘要

**修复日期**: 2026-02-24  
**修复范围**: `turing_test/frontend/src/`  
**整体进度**: 11/16 任务完成 (68.75%)

---

## 已完成修复（P0 + P1）

### 严重问题（🔴 P0）- 3/3 完成

| 编号 | 任务 | 状态 | 修改文件 |
|------|------|------|---------|
| P001 | 添加结束对话按钮 | ✅ | `ChatHeader.vue`, `Chat.vue` |
| P002 | 实现最低轮数检查 | ✅ | `ChatHeader.vue`, `Chat.vue` |
| P003 | 完善昵称验证 | ✅ | `Login.vue`, `constants.ts` |

### 中等问题（🟡 P1）- 8/8 完成

| 编号 | 任务 | 状态 | 修改文件 |
|------|------|------|---------|
| P004 | 修正消息长度限制 | ✅ | `ChatInput.vue`, `constants.ts` |
| P005 | 统一元对话关键词 | ✅ | `constants.ts` |
| P006 | 添加场中判断常驻按钮 | ✅ | `ChatHeader.vue` |
| P007 | 实现对手头像 Hash 颜色 | ✅ | `MessageList.vue`, `formatters.ts` |
| P008 | 实现轮流发送机制 | ✅ | `game.ts`, `Chat.vue`, `ChatInput.vue` |
| P009 | 添加正在输入提示 | ✅ | `game.ts`, `Chat.vue` |
| P010 | 添加返回按钮 | ✅ | `ChatHeader.vue` |
| P011 | 添加敏感词过滤 | ✅ | `ChatInput.vue`, `constants.ts` |

### 细节优化（🟢 P2）- 0/5 完成

| 编号 | 任务 | 状态 |
|------|------|------|
| P012 | 统一信心等级文案 | ⏳ 待开始 |
| P013 | 优化响应式布局 | ⏳ 待开始 |
| P014 | 改为相对时间显示 | ⏳ 待开始 |
| P015 | 优化空状态提示 | ⏳ 待开始 |
| P016 | 统一加载组件 | ⏳ 待开始 |

---

## 详细修改说明

### 1. constants.ts 修改

**新增常量**：
```typescript
export const MAX_MESSAGE_LENGTH = 50 // 消息最大长度

// 元对话关键词（与文档保持一致）
export const META_KEYWORDS = [
  '真人', '机器', 'AI', '机器人', '人工智能',
  '程序', '算法', '人类', '人', '电脑', '计算'
]

// 敏感词列表
export const SENSITIVE_WORDS = [
  '微信', 'QQ', '电话', '手机', '邮箱', '邮件',
  'http://', 'https://', 'www.', '.com', '.cn',
  '傻', '笨', '猪', '滚', '废物', '白痴'
]
```

### 2. ChatInput.vue 修改

**主要改动**：
- 移除 `maxLength` prop，改用常量 `MAX_MESSAGE_LENGTH`
- 删除本地元对话关键词列表，使用统一常量
- 添加敏感词检测函数 `checkSensitiveWords()`
- 发送前进行敏感词过滤

**关键代码**：
```typescript
// 检测敏感词
function checkSensitiveWords(text: string): string | null {
  for (const word of SENSITIVE_WORDS) {
    if (text.includes(word)) {
      return word
    }
  }
  return null
}

// 发送消息时检测
const sensitiveWord = checkSensitiveWords(content)
if (sensitiveWord) {
  ElMessage.warning(`消息包含敏感词：${sensitiveWord}`)
  return
}
```

### 3. game.ts 修改

**新增状态**：
```typescript
// 轮流发送状态
const isUserTurn = ref<boolean>(true)

// 正在输入状态
const isOpponentTyping = ref<boolean>(false)
```

**新增方法**：
```typescript
function setUserTurn(turn: boolean) {
  isUserTurn.value = turn
}

function setOpponentTyping(typing: boolean) {
  isOpponentTyping.value = typing
}

function reset() {
  // ...
  isUserTurn.value = true
  isOpponentTyping.value = false
}
```

**消息添加时自动切换回合**：
```typescript
function addMessage(message: MessageDisplay) {
  // ...
  if (message.sender === 'user') {
    isUserTurn.value = false // 用户发送后，轮到对手
  } else {
    isUserTurn.value = true // 对手发送后，轮到用户
    isOpponentTyping.value = false
  }
}
```

### 4. ChatHeader.vue 修改

**新增按钮**：
- 返回按钮（`← 返回`）
- 场中判断按钮（`⚡ 场中判断`）
- 结束对话按钮（`结束对话`）

**结束对话逻辑**：
```typescript
async function handleEndChat() {
  // 检查最低轮数
  const currentTurns = Math.floor(gameStore.turn / 2)
  if (currentTurns < MIN_CHAT_TURNS) {
    ElMessage.warning(`请多聊几句再结束哦（至少${MIN_CHAT_TURNS}轮，当前${currentTurns}轮）`)
    return
  }
  
  // 确认对话框
  await ElMessageBox.confirm('确定要结束这次对话吗？', '结束对话', {...})
  
  // 调用后端接口
  await endSession(gameStore.sessionId)
  router.push('/survey')
}
```

### 5. Chat.vue 修改

**新增正在输入提示 UI**：
```vue
<!-- 正在输入提示 -->
<div v-if="gameStore.isOpponentTyping" class="typing-indicator">
  <div class="typing-dots">
    <span class="typing-dot"></span>
    <span class="typing-dot"></span>
    <span class="typing-dot"></span>
  </div>
  <span class="typing-text">对方正在输入...</span>
</div>
```

**输入框禁用逻辑**：
```vue
<ChatInput
  :disabled="!gameStore.isUserTurn || isLoading"
  :placeholder="gameStore.isUserTurn ? '输入消息...' : '等待对方发送...'"
  @send="handleSendMessage"
/>
```

**WebSocket 消息处理**：
```typescript
on('typing', () => {
  gameStore.setOpponentTyping(true)
})

on('stop_typing', () => {
  gameStore.setOpponentTyping(false)
})
```

### 6. Login.vue 修改

**新增默认昵称生成**：
```typescript
const defaultNickname = `访客#${Math.floor(100 + Math.random() * 900)}`
```

**新增昵称验证函数**：
```typescript
function validateNickname(nick: string): boolean {
  // 长度检查
  if (nick.length < 2 || nick.length > 20) {
    errorMessage.value = '昵称长度应为 2-20 个字符'
    return false
  }
  
  // 格式检查
  const nicknameRegex = /^[\u4e00-\u9fa5a-zA-Z0-9#]+$/
  if (!nicknameRegex.test(nick)) {
    errorMessage.value = '昵称仅支持中文、英文、数字和#符号'
    return false
  }
  
  return true
}
```

**验证邀请码后自动填充默认昵称**：
```typescript
async function verifyCode() {
  // ...
  step.value = 2
  nickname.value = defaultNickname
}
```

### 7. formatters.ts 修改

**新增工具函数**：
```typescript
/**
 * 根据字符串生成 Hash 颜色（HSL）
 */
export function stringToColor(str: string): string {
  let hash = 0
  for (let i = 0; i < str.length; i++) {
    hash = str.charCodeAt(i) + ((hash << 5) - hash)
  }
  
  const hue = Math.abs(hash) % 360
  return `hsl(${hue}, 70%, 50%)`
}

/**
 * 获取名称首字母（大写）
 */
export function getInitials(name: string): string {
  if (!name) return '?'
  return name.charAt(0).toUpperCase()
}
```

### 8. MessageList.vue 修改

**新增头像显示**：
```vue
<div class="message-header-row">
  <div 
    class="avatar" 
    :style="{ backgroundColor: getAvatarColor(message.sender) }"
  >
    {{ getInitials(message.sender === 'user' ? '我' : '对手') }}
  </div>
  
  <div class="message-header">
    <span class="sender-name">...</span>
    <span class="message-time">...</span>
  </div>
</div>
```

**头像颜色生成**：
```typescript
function getAvatarColor(sender: string): string {
  return stringToColor(sender === 'user' ? 'user' : 'opponent')
}
```

---

## 修改文件清单

| 文件路径 | 修改类型 | 说明 |
|---------|---------|------|
| `utils/constants.ts` | 修改 | 新增 `MAX_MESSAGE_LENGTH`、`SENSITIVE_WORDS`，修正 `META_KEYWORDS` |
| `components/Chat/ChatInput.vue` | 重写 | 消息长度限制、敏感词过滤 |
| `stores/game.ts` | 重写 | 轮流发送、正在输入状态 |
| `components/Chat/ChatHeader.vue` | 重写 | 返回按钮、场中判断按钮、结束对话按钮 |
| `views/Chat.vue` | 重写 | 正在输入提示、轮流发送逻辑 |
| `views/Login.vue` | 重写 | 昵称验证、默认昵称生成 |
| `utils/formatters.ts` | 修改 | 新增 `stringToColor()`、`getInitials()` |
| `components/Chat/MessageList.vue` | 重写 | 头像显示、Hash 颜色 |

---

## 验收测试建议

### 功能测试

1. **结束对话功能**
   - [ ] 聊天界面显示"结束对话"按钮
   - [ ] 少于 3 轮时点击结束显示警告
   - [ ] 达到 3 轮后点击结束显示确认对话框
   - [ ] 确认后跳转到问卷页面

2. **昵称验证**
   - [ ] 输入 1 字符昵称显示错误
   - [ ] 输入特殊符号显示错误
   - [ ] 验证邀请码后自动填充"访客#XXX"
   - [ ] 正常昵称可以成功登录

3. **消息发送**
   - [ ] 输入超过 50 字符无法输入
   - [ ] 发送敏感词显示警告
   - [ ] 发送消息后输入框禁用（轮流发送）
   - [ ] 收到对方消息后输入框启用

4. **场中判断**
   - [ ] 聊天界面显示"场中判断"按钮
   - [ ] 点击弹出判断弹窗
   - [ ] 已使用后按钮禁用

5. **正在输入提示**
   - [ ] 对方输入时显示"对方正在输入..."
   - [ ] 显示三个跳动的圆点动画
   - [ ] 对方发送消息后隐藏提示

6. **头像显示**
   - [ ] 用户和对手显示不同颜色头像
   - [ ] 头像显示名称首字母
   - [ ] 同一会话中颜色保持一致

---

## 遗留问题（P2 待优化）

| 编号 | 问题 | 优先级 | 预计工时 |
|------|------|--------|---------|
| P012 | 统一信心等级文案 | 🟢 低 | 0.5h |
| P013 | 优化响应式布局 | 🟢 低 | 2h |
| P014 | 改为相对时间显示 | 🟢 低 | 0.5h |
| P015 | 优化空状态提示 | 🟢 低 | 1h |
| P016 | 统一加载组件 | 🟢 低 | 1h |

---

## 下一步计划

1. **自测阶段**：开发者进行功能自测
2. **测试阶段**：QA 进行完整回归测试
3. **P2 优化**：根据时间决定是否实施细节优化
4. **上线部署**：测试通过后部署到生产环境

---

**报告编制**: AI Assistant  
**日期**: 2026-02-24  
**版本**: 1.0
